pipeline { 
    agent {label "t1"}
    stages {
        stage('Build') { 
            steps { 
                sh 'echo "make build stage bbb..."> /tmp/gittest' 
            }
        }
        stage('Test'){
            steps {
                sh 'echo "make check, test stage ...">> /tmp/gittest'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "make publish, deploy stage ...">> /tmp/gittest'
            }
        }
    }
}
