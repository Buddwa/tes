Jenkinsfile (Declarative Pipeline)
pipeline { 
    agent {label "t1"}
    stages {
        stage('Build') { 
            steps { 
                sh 'echo "make build stage bbb..."' 
            }
        }
        stage('Test'){
            steps {
                sh 'echo "make check, test stage ..."'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "make publish, deploy stage ..."'
            }
        }
    }
}
