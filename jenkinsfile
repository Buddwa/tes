Jenkinsfile (Declarative Pipeline)
pipeline { 
    agent t1
    stages {
        stage('Build') { 
            steps { 
                sh 'echo "make build stage aaa..."' 
            }
        }
        stage('Test'){
            steps {
                sh 'echo "make check, test stage ..."'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "make publish, deploy stage ..."'
            }
        }
    }
}
